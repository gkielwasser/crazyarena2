#ifndef MAP_H_INCLUDED
#define MAP_H_INCLUDED

class Map {
	protected:
		// liste de cubes

	public:
		   Map();
		   Map(const Map &);
		   Map();
		   ~Map();

		   void draw() {

		   }
};

#endif // Map_H_INCLUDED
